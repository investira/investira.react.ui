import { default as MuiCardHeader } from "@mui/material/CardMedia";

function CardHeader(props) {
  return <MuiCardHeader {...props} />;
}

CardHeader.displayName = "CardHeader";

export default CardHeader;
